<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo001">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo001">
        <activity android:name=".DemoNhanVienActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DemoIntentActivity"></activity>
        <activity android:name=".DemoListViewActivity">

        </activity>
        <activity android:name=".DemoImage" />
        <activity android:name=".DemoLinearLayoutActivity" />
        <activity android:name=".DemoRelativeLayoutActivity" />
        <activity android:name=".SecondScreenActivity" />
        <activity android:name=".FirstScreenActivity" />
        <activity android:name=".BeLamToanActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>